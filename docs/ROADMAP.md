# Roadmap

Skoobi Agent is currently installable from GitHub Releases.

Planned work:

- npm `create-skoobi` installer package
- Homebrew tap for macOS
- dependency audit review
- provider observability dashboard
- memory provenance migration tooling
- compatibility namespace cleanup for remaining historical ClaudeClaw identifiers

Some internal compatibility identifiers still use the historical ClaudeClaw namespace. The public product identity is Skoobi Agent. A compatibility-safe rename is planned separately.

No dates are promised. These items may change.

